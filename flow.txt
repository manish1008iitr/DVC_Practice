1. Creat git repo and clone it in local 
2. Create code.py and add code to it (It will save a scv dile to a new data folder )
3. Do a git commit and push before initializing dvc 
#pip install dvc 
# Now we will do "dvc init" (creates .dvcignore)
Now we do mkdir S3 (creates a news s3 directory)
Now we do "dvc remote add -d myremote S3"
Next "dvc add data/"

Now it will aks to do : "git rm -r --cached 'data'" and git commit -m "Stop tracking data"
Because intially we were tracking data folder from git and so now we need to remove it from git 

Again we do "dvc add data" (creates own .dvc)
Then git add .gitignore data.dvc 
Now we do dvc commit and then dvc push 

Do a git add-commit-push to mark this stage as first version of data 
Again dvc commit and then dvc push 
